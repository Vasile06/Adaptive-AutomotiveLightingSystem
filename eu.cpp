

// main.cpp
#include <iostream>
//#include"pch.h"

int main()

{
	std::cout << "Heloo" << std::endl;
	//	Manager_Lights m_l{};
	//
	//	m_l.SetButton(ButtonState::Off);
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//	m_l.SetButton(ButtonState::LowBeam);
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//
	//	m_l.SetButton(ButtonState::HighBeam);
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//	m_l.SetButton(ButtonState::Automatic);
	//	m_l.SetIllumination(0.0); // day
	//	m_l.SetFrontal(10.0); // does not matter
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//	m_l.SetButton(ButtonState::Automatic);
	//	m_l.SetIllumination(2.0); // night
	//	m_l.SetFrontal(60.0); // far away
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//	m_l.SetFrontal(10.0); // closer
	//	std::cout << "Display color: " << std::get<0>(m_l.CalculateLightsState()) << std::endl;
	//	std::cout << "Light State: " << std::get<1>(m_l.CalculateLightsState()) << std::endl;
	//
	//
	//	Manager_Actuators m_a{ m_l };
	//	m_a.SetDisplay();
	//	m_a.SetLights();
	//
	//	std::cout << "Display color: " << m_a.GetDisplay().GetLedColor() << std::endl;
	//	std::cout << "Light State: " << m_a.GetLights().GetLights() << std::endl;
}